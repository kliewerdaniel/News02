# Minimal requirements for News02 Web Interface
# Use this if the full requirements.txt fails

# Web Framework
flask>=2.3.0
werkzeug>=2.3.0
jinja2>=3.1.0

# Core Dependencies  
feedparser>=6.0.0
pyyaml>=6.0
python-dotenv>=1.0.0
requests>=2.28.0
tqdm>=4.64.0

# Basic XML/HTML processing
lxml>=4.9.0

# Optional TTS (can be installed separately)
# edge-tts>=6.1.0

# Optional LLM clients (can be installed separately)
# openai>=1.0.0
# ollama>=0.1.0
# google-generativeai>=0.3.0

# Heavy ML dependencies (install separately if needed)
# torch>=2.0.0
# transformers>=4.30.0
# newspaper3k>=0.2.8